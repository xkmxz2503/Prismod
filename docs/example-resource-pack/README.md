# Prismod 示例资源包

这是一个最小可用的自定义滤镜资源包，提供 `example:debug` 反色滤镜。

将此目录压缩为 ZIP，或直接放入 Minecraft 实例的 `resourcepacks` 目录，然后在游戏中启用并执行资源重载。

资源文件位于 `assets/example/shaders/post/debug.json`。启用后可在 Prismod 滤镜设置中看到 `example:debug`。
